name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  # Dependencies
  - numpy

    # Pip-only installs
  #- pip:
  #  - codecov

# channels : stuff like conda-forge
